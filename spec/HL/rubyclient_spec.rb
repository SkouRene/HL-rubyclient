require "spec_helper"

describe HL::Rubyclient do
  it "has a version number" do
    expect(HL::Rubyclient::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
